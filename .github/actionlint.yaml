self-hosted-runner:
  # Labels of self-hosted runner in array of string
  labels:
    [
      azure-cvm,
      arc-runner-set,
      bazel-cached,
      bazel-nocache,
      ubuntu-latest-8-cores,
    ]
