name: Select Release Manager

on:
  workflow_dispatch:

jobs:
  test-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    - run: python -c "import random; print(random.choice(['daniel', 'nils', 'leonard', 'fabian', 'paul', 'leon', 'malte', 'thomas', 'otto']))"
