genrule(
    name = "tfsec_bin",
    srcs = ["tfsec"],
    outs = ["tfsec_bin_out"],
    cmd = "cp $< $@",  # Copy the binary to the output directory.
    executable = True,
    visibility = ["//visibility:public"],
)
