genrule(
    name = "actionlint_bin",
    srcs = ["actionlint"],
    outs = ["actionlint_bin_out"],
    cmd = "cp $< $@",  # Copy the binary to the output directory.
    executable = True,
    visibility = ["//visibility:public"],
)
