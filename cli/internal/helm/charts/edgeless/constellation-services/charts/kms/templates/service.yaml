apiVersion: v1
kind: Service
metadata:
  name: kms
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: grpc
    port: {{ .Values.port }}
    protocol: TCP
    targetPort: {{ .Values.port }}
  selector:
    k8s-app: kms
  type: ClusterIP
status:
  loadBalancer: {}
