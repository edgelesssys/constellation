apiVersion: v1
kind: Service
metadata:
  name: kms
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: grpc
    port: {{ .Values.kmsPort }}
    protocol: TCP
    targetPort: {{ .Values.kmsPort }}
  selector:
    k8s-app: kms
  type: ClusterIP
status:
  loadBalancer: {}
