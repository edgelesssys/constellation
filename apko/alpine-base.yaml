contents:
  repositories:
    - https://dl-cdn.alpinelinux.org/alpine/v3.17/main
  packages:
    - alpine-base

entrypoint:
  command: /bin/sh -l

environment:
  PATH: /usr/sbin:/sbin:/usr/bin:/bin

archs:
  - amd64
  - 386
