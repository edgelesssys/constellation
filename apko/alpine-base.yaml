contents:
  keyring:
    - index-signing-key.rsa.pub
  repositories:
    - "@local repository-apk"
  packages:
    - alpine-base@local

entrypoint:
  command: /bin/sh -l

environment:
  PATH: /usr/sbin:/sbin:/usr/bin:/bin

archs:
  - amd64
