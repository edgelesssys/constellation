contents:
  keyring:
    - index-signing-key.rsa.pub
  repositories:
    - "@local repository-apk"
  packages:
    - alpine-base@local
    - cryptsetup-dev@local
    - gcompat@local

environment:
  PATH: /usr/sbin:/sbin:/usr/bin:/bin

archs:
  - amd64
